{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/dotnet/hashcode/hashcode.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}